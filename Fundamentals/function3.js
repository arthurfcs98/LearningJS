// put a function on a var

const printSoma = function (a, b) {
    console.log(a + b)
}

printSoma(2 , 6)

// arrow function

const soma = (a, b) => {
    return a + b
}

console.log(soma(3,7))

// Implicit return 
const subtraction = (a, b) => a - b

console.log(subtraction(4,69))
